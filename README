📋 Lista de Tarefas – API REST com Spring Boot

Este projeto é uma API REST para gerenciamento de tarefas, desenvolvida como parte de um Projeto de Extensão, com o objetivo de aplicar na prática os conceitos de desenvolvimento backend utilizando Java e Spring Boot.

A aplicação permite que usuários cadastrem e visualizem tarefas, servindo como uma base didática para o estudo de APIs REST, persistência de dados e organização em camadas.

🚀 Tecnologias Utilizadas

Java

Spring Boot

Spring Web

Spring Data JPA

H2 Database

Maven

Tomcat Embutido

📂 Organização do Projeto

O projeto foi estruturado seguindo boas práticas de separação de responsabilidades, com pacotes distintos para:

Controller (camada de entrada da API)

Model (entidades do sistema)

Repository (acesso a dados)

Service (regra de negócio – em evolução)

Classe principal de inicialização da aplicação

🧱 Etapas Desenvolvidas
1️⃣ Criação do Projeto

O projeto foi criado utilizando Spring Boot, com as dependências necessárias para criar uma API REST e integrar um banco de dados relacional em memória.

2️⃣ Inicialização da Aplicação

Foi definida uma classe principal responsável por iniciar a aplicação Spring Boot, permitindo que o servidor embutido execute a aplicação localmente.

3️⃣ Modelagem da Entidade Tarefa

Foi criada a entidade que representa uma tarefa no sistema.
Atualmente, cada tarefa possui:

Identificador único

Descrição

Status de conclusão

Prioridade

Data de criação automática

Essa modelagem permite evoluir o sistema de forma organizada.

4️⃣ Persistência com JPA

O projeto utiliza Spring Data JPA para realizar a persistência das tarefas no banco de dados, abstraindo operações como salvar e listar registros.

5️⃣ Criação da API REST

Foi criado um controller REST responsável por expor os endpoints da aplicação, permitindo:

Inserir novas tarefas

Listar todas as tarefas cadastradas

A comunicação é feita via JSON.

🗄️ Banco de Dados H2

O banco de dados H2 foi configurado para facilitar o desenvolvimento e os testes, eliminando a necessidade de um banco externo.
O console web do H2 permite visualizar as tabelas e dados gerados pela aplicação.

📈 Estado Atual do Projeto

API REST funcional

Cadastro de tarefas ativo

Listagem de tarefas funcionando

Persistência com banco H2

Nível do projeto: Iniciante para Intermediário
Status: Em desenvolvimento

🔮 Próximas Evoluções Planejadas

Buscar tarefa por identificador

Atualizar tarefas existentes

Marcar tarefas como concluídas

Remover tarefas

Validações de dados

Padronização de prioridade

Implementação completa da camada de serviço

👨‍💻 Contexto Acadêmico

Este projeto foi desenvolvido com foco educacional, como parte de um Projeto de Extensão, visando consolidar conhecimentos em Java, Spring Boot e desenvolvimento de APIs REST.